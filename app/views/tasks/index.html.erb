<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
<li>
  <input type="checkbox" value="<%= task.title %>" <%= 'checked' if task.completed %>>
  <%= link_to task.title, task_path(task) %>
</li>

<% end %>
</ul>


